# --- Core data / ML (crowd prediction, LightGBM) ---
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
lightgbm>=4.0.0
joblib>=1.3
matplotlib>=3.8
tabulate>=0.9.0

# --- Core data / ML (weather forecasting, XGBoost -- see weather_ml/) ---
xgboost>=2.0
seaborn>=0.13

# --- API service ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.5
requests>=2.31

# --- Optional: Redis-backed cache (app/cache.py falls back to in-memory
#     automatically if REDIS_URL is unset or redis-py isn't installed) ---
# redis>=5.0
