# Base URL of the Tour-Weave FastAPI backend. Vite only exposes env vars
# prefixed with VITE_. Copy this file to .env and adjust if your backend
# runs on a different host/port.
VITE_API_BASE_URL="http://localhost:8000"
